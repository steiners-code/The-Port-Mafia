name: Deploy The Port Mafia

on:
  push:
    branches:
      - master

jobs:
  deploy:
    name: Build & Run Containerized Stack
    runs-on: [self-hosted, steiners-local]

    steps:
      - name: Checkout Repository Source
        uses: actions/checkout@v4

      - name: Create Frontend Environment File
        run: |
          mkdir -p frontend
          cat <<EOF > frontend/.env
          JWT_PUBLIC_KEY="${{ secrets.JWT_PUBLIC_KEY }}"
          NEXT_PUBLIC_HOME_URL="${{ secrets.HOME_URL }}"
          NEXT_PUBLIC_SERVER_URL="${{ secrets.SERVER_URL }}"
          NEXT_PUBLIC_FRONTEND_URL="${{ secrets.FRONTEND_URL }}"
          NEXT_PUBLIC_DEFAULT_REDIRECT_URL="${{ secrets.FRONTEND_URL }}"
          NEXT_PUBLIC_LINKEDIN_CLIENT_ID="${{ secrets.LINKEDIN_CLIENT_ID }}"
          NODE_ENV="production"
          EOF

      - name: Create API Gateway Environment File
        run: |
          mkdir -p backend/api-gateway
          cat <<EOF > backend/api-gateway/.env
          JWT_PUBLIC_KEY="${{ secrets.JWT_PUBLIC_KEY }}"
          NODE_ENV="development"
          EOF

      - name: Create LinkedIn Environment File
        run: |
          mkdir -p backend/linkedin
          cat <<EOF > backend/linkedin/.env
          DATABASE_URL="${{ secrets.LINKEDIN_DATABASE_URL }}"
          LINKEDIN_CLIENT_ID="${{ secrets.LINKEDIN_CLIENT_ID }}"
          LINKEDIN_CLIENT_SECRET="${{ secrets.LINKEDIN_CLIENT_SECRET }}"
          ENCRYPTION_KEY="${{ secrets.ENCRYPTION_KEY }}"
          CRON_API_SECRET="${{ secrets.CRON_API_SECRET }}"
          NODE_ENV="development"
          EOF

      - name: Create Main Environment File
        run: |
          mkdir -p backend/main
          cat <<EOF > backend/main/.env
          JWT_PRIVATE_KEY="${{ secrets.JWT_PRIVATE_KEY }}"
          DATABASE_URL="${{ secrets.MAIN_DATABASE_URL }}"
          FRONTEND_URL="${{ secrets.FRONTEND_URL }}"
          HOME_URL="${{ secrets.HOME_URL }}"
          NODE_ENV="development"
          EOF

      - name: Standardize and Rebuild Containers
        run: |
          docker compose down --remove-orphans
          docker compose up --build -d
          docker image prune -f