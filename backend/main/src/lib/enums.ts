export enum APPTYPE {
    LINKEDIN,
    X,
    REDDIT,
    FACEBOOK,
    INSTAGRAM,
    THREADS,
    TIKTOK,
}

export enum APPSTATUS {
    CONNECTED,
    DISCONNECTED
}

export enum LOGLEVEL {
    ERROR = "ERROR",
    INFO = "INFO",
    WARN = "WARN"
}
